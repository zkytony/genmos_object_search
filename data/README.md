# Download Dataset

This will download the **SL\_OSM\_Dataset**, part of the SLOOP project.

First, install `gdown`:
```
pip install gdown
```

Then, run the download script:
```
python download.py
```
